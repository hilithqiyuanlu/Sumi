import 'package:flutter/material.dart';

import '../memory/memory_center_page.dart';

/// Kept only for old routes; memory management now lives in MemoryCenterPage.
class UserHypothesesPage extends StatelessWidget {
  const UserHypothesesPage({super.key});

  @override
  Widget build(BuildContext context) => const MemoryCenterPage();
}
