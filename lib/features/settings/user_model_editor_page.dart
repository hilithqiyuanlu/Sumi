import 'package:flutter/material.dart';

import '../memory/memory_center_page.dart';

/// Kept only for old routes; USER_MODEL.md is now an export, not an editor.
class UserModelEditorPage extends StatelessWidget {
  const UserModelEditorPage({super.key});

  @override
  Widget build(BuildContext context) => const MemoryCenterPage();
}
