name: sumi
description: "Sumi（米糖）— 本地优先的自学个人助手"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.0-134.0.dev

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.4
  path: ^1.9.0
  sqflite: ^2.3.3+1
  flutter_secure_storage: ^9.2.4
  http: ^1.2.2
  speech_to_text: ^7.0.0
  flutter_markdown: ^0.7.6
  crypto: ^3.0.6
  llamadart: ^0.8.16
  llamadart_llama_cpp_flutter: ^0.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  sqflite_common_ffi: ^2.3.6

flutter:
  uses-material-design: true
